import CButton from '@/components/Button';

export default function PostButton() {
    return (
        <div className="w-[calc(100%-32px)] fixed bottom-10">
            <CButton>등록하기</CButton>
        </div>
    );
}
